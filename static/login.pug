doctype html
head
    include Component/Head.pug
    link(rel='stylesheet' href='style/login.css')
section.section-login
  .section-login__welcome
    h1 Добро Пожаловать
    object(data='img/img_login.svg' type='image/svg+xml')
  .section-login__form
    .section-login__form-content
      form#formData
        .form
          label
            p Логин
            input#login(type='text' placeholder='Введите логин' name='login')
          label
            p Пароль
            input#password(type='text' placeholder='Введите пароль' name='password')
          p.error_login
          button(type="submit")
            | Авторизоваться
          a(href='/signin') Нет аккаунта ?
script 
  include js/login.js
